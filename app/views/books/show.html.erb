<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>

<nav class="page-nav">
  <%= link_to t('links.edit', model: Book.model_name.human), edit_book_path(@book) %> |
  <%= link_to t('links.index', model: Book.model_name.human(count: 2)), books_path %>

  <%= button_to t('.delete'), @book, method: :delete %>
</nav>
